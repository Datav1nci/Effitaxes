{
  "compilerOptions": {                        //These are settings that tell TypeScript how to process your code
    "target": "ES2017",                       //"ES2017": Compiles your code to work with JavaScript features from 2017
    "lib": ["dom", "dom.iterable", "esnext"], //Includes libraries like the DOM (for web browsers) and modern JavaScript features.
    "allowJs": true,                          //Allows JavaScript files to be used alongside TypeScript.
    "skipLibCheck": true,                     //Skips type checking for external libraries to speed up compilation.
    "strict": true,                           //Enables strict type checking to catch more errors.
    "noEmit": true,                           //Prevents TypeScript from outputting compiled files (Next.js handles this)
    "esModuleInterop": true,                  //Improves compatibility with different module systems
    "module": "esnext",                       //Uses the latest module system.
    "moduleResolution": "bundler",            //Tells TypeScript to resolve modules like a bundler (e.g., Webpack, used by Next.js).
    "resolveJsonModule": true,                //Allows importing JSON files as modules
    "isolatedModules": true,                  //Ensures each file is treated independently, which works well with Next.js.
    "jsx": "preserve",                        //Keeps JSX (used in React components) as-is for Next.js to handle.
    "incremental": true,                      //Speeds up future builds by reusing previous results.
    "plugins": [                              //Adds Next.js-specific TypeScript support
      {
        "name": "next"
      }
    ],
    "paths": {                         //Sets up an alias so you can import from @/ (e.g., @/components) to point to the src folder.
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"], //Lists files TypeScript should check, like next-env.d.ts (for Next.js environment types), all .ts and .tsx files, and types generated by Next.js.
  "exclude": ["node_modules"] //Tells TypeScript to ignore the node_modules folder, which contains external libraries.
}
//tsconfig.json 
//This file is a configuration for TypeScript, 
//which is used in your Next.js project to add static typing and better code checking.
//This setup is typical for a Next.js project using TypeScript, 
//ensuring type safety and compatibility with the framework's features.
//tsconfig.json lives at the root to manage the entire codebase.